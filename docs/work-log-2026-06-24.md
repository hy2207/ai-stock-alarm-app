# 작업 로그 — 2026-06-24

## 완료한 작업

### 1. DB 마이그레이션 (INFRA-004)
- SQLite → PostgreSQL(Supabase) 전환
- `prisma/schema.prisma`: provider `postgresql`, `directUrl` 추가
- `prisma/migrations/20260624000000_init_postgresql/migration.sql`: PostgreSQL 전용 baseline 마이그레이션 작성
- `src/lib/prisma.ts`: pgBouncer 파라미터 자동 주입 (`buildDatasourceUrl()`)
- `package.json` build 스크립트: `prisma generate && next build` (Vercel 캐시 이슈 해결)

**환경변수:**
```
DATABASE_URL=postgresql://.../postgres?pgbouncer=true  (포트 6543, 트랜잭션 모드)
POSTGRES_URL=postgresql://.../postgres                  (포트 5432, 직접 연결)
```

---

### 2. 프로덕션 로그인 오류 수정
- `src/lib/auth/authOptions.ts`: KakaoProvider 환경변수 없을 때 조건부 포함으로 수정
- Google Cloud Console에서 OAuth 콜백 URL 등록

---

### 3. UX 버그 수정
- **관심종목 저장 후 페이지 이동 안 되는 문제**: `router.replace()` + `router.refresh()` 경쟁 조건 → `window.location.replace()`로 교체 (`src/app/components/WatchlistPickerForm.tsx`)
- **재로그인 후 추천 카드 없는 문제**: 프로덕션 추천 생성 cron 누락 → `/api/cron/generate-recommendations` 신규 생성 (`vercel.json`에 UTC 20:00 스케줄 추가)

---

### 4. 헬스체크 + 보안 헤더 (MON-002, MON-005)
- `src/lib/admin/health.ts`: `checkDb()` 추가 — DB ping(`SELECT 1`) 후 `connected`, `latencyMs` 반환
- `src/lib/dto/healthResponse.ts`: `db` 필드 스키마 추가
- `next.config.mjs`: HTTP 보안 헤더 추가 (X-Frame-Options, HSTS, CSP 등)

---

### 5. OneSignal 푸시 설정 (MON-004)
- OneSignal 앱 생성 완료
  - **App ID**: `28ed3bcb-ce6a-4ed7-b292-cfd674a81b4c`
  - **Site URL**: `https://ai-stock-alarm-hwayoung-s-projects.vercel.app`
- `public/OneSignalSDKWorker.js`: 서비스 워커 파일 추가 (v16 필수)
- Vercel 환경변수 등록: `NEXT_PUBLIC_ONESIGNAL_APP_ID`, `ONESIGNAL_APP_ID`, `ONESIGNAL_REST_API_KEY`

---

### 6. 프로덕션 도메인 설정
- 고정 URL 설정: `https://ai-stock-alarm-hwayoung-s-projects.vercel.app`
- `NEXTAUTH_URL`, `NEXT_PUBLIC_APP_URL` Vercel 환경변수 업데이트

---

## 닫은 GitHub 이슈
| 이슈 | 제목 | 상태 |
|------|------|------|
| #13 INFRA-004 | Supabase 설정 | 완료 |
| #28 MON-002 | Vercel Dashboard 모니터링 | 완료 |
| #29 MON-005 | 보안 모니터링 | 완료 |
| #31 SEC-002 | 인증 보안 | 완료 |
| #32 SEC-003 | 데이터 보안 | 완료 |
| #19 AVAIL-001 | 가용성 | 완료 |
| #67 SEC-005 | 로그 보존 정책 | 완료 (가이드 제공) |
| #118 MON-003 | PostHog KPI 대시보드 | 완료 (가이드 제공) |
| #75 MON-004 | OneSignal 모니터링 | 완료 (설정 완료) |

---

## 내일 확인 필요

### OneSignal 브라우저 알림 팝업 미표시 문제

**증상**: 프로덕션 접속 시 브라우저 알림 허용 팝업이 뜨지 않음

**체크리스트:**
1. 브라우저 콘솔에서 OneSignal 오류 메시지 확인
2. `https://ai-stock-alarm-hwayoung-s-projects.vercel.app/OneSignalSDKWorker.js` 접속 — 파일 내용이 보여야 함
3. OneSignal 대시보드 → Settings → Web Configuration → Site URL이 `https://ai-stock-alarm-hwayoung-s-projects.vercel.app`인지 확인
4. `NEXT_PUBLIC_ONESIGNAL_APP_ID` Vercel 환경변수가 production에 등록됐는지 확인
5. Chrome DevTools → Application → Service Workers에서 OneSignalSDKWorker 등록 여부 확인

**Google OAuth도 업데이트 필요:**
- Google Cloud Console → OAuth 2.0 클라이언트 → Authorized redirect URIs에 추가:
  `https://ai-stock-alarm-hwayoung-s-projects.vercel.app/api/auth/callback/google`
